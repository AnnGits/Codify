Базовые команды подразумевают краткое описание шаблона, который необходимо получить

1. пересчитать, буквы - получает на вход данные *что пересчитать*: 
если используется слово "буквы" - считает всё, если просто прописывается "буква" - считает кол-во её вхождений

2. удалить, символ, в последовательности, количество раз

3. удалить, символы, одинаковые, в последовательности

4. создать, список, *имя списка*, *сам список (вписывать с пробелами пробелов между значениями)* - 
на выходе получается словарь, из которого, обращаясь по заданному имени, можно получить нужный список

5. пересчитать, вариации, различны (шаблон позволет найти количество всех перестановок символов в 
строке при том, что все символы различны)

6. пересчитать, вариации, повторяются (шаблон позволет найти количество всех перестановок символов в 
строке при том, что символы могут повторяться)

7. программа, калькулятор

8. прогр@амма, окно (шаблон создания окна)

9. программа, окно, базовые виджеты (шаблон создания виджетов окна (надпись, поле ввода, кнопка))

10. добавить, команда